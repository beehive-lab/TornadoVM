# Maven configuration for TornadoVM
# These flags are applied to all Maven commands automatically

# Parallel build with 1.5 threads per CPU core (same as make default)
-T1.5C

# Enable colored output for better readability
-Dstyle.color=always

# Default profiles: JDK 21 + OpenCL backend
# Override by passing different -P flag on command line
-Pjdk21,opencl-backend

# Common Maven properties for faster builds
# Skip Javadoc generation by default (can be enabled with -Dmaven.javadoc.skip=false)
-Dmaven.javadoc.skip=true

# Fail fast on first module failure (faster feedback during development)
-Dfailfast=true

# Show timestamps in build output
-Dorg.slf4j.simpleLogger.showDateTime=true
-Dorg.slf4j.simpleLogger.dateTimeFormat=HH:mm:ss
